.CODE

main PROC
    ;initialize data segment
    MOV AX, @DATA
    MOV DS, AX
	;preserve base pointer
    PUSH BP
    MOV BP, SP
    SUB SP, 2    ; Line 10
    MOV AX, 7
    PUSH AX
    PUSH BX
    PUSH CX
    PUSH DX
    CALL func    ; Line 11
    PUSH AX
    POP DX
    POP CX
    POP BX
    POP AX    ; Line 11
    MOV [BP - 0], AX    ; Line 11
    MOV AX, [BP - 0]    ; Line 12
    CALL print_output    ; Line 12
    CALL new_line    ; Line 12
	;restore stack pointer
    ADD SP, 2
	;restore base pointer
    POP BP
    ;exit program
    MOV AX,4CH
    INT 21H
main ENDP

func PROC
	;preserve base pointer
    PUSH BP
    MOV BP, SP
    SUB SP, 2    ; Line 2
	L0:
    MOV BX, 0    ; Line 3
    MOV AX, [BP + 4]    ; Line 3
    CMP AX, BX    ; Line 3
	JE L1
    JMP L2
    L1:
    MOV AX, 0    ; Line 3
	;restore stack pointer
    ADD SP, 2
	;restore base pointer
    POP BP
    RET 2
    JMP L3
    L2:
    L3:
    MOV AX, [BP + 4]    ; Line 4
    MOV [BP - 0], AX    ; Line 4
    MOV BX, 1    ; Line 5
    MOV AX, [BP + 4]    ; Line 5
    SUB AX, BX    ; Line 5
    PUSH AX    ; Line 5
    POP AX
    PUSH AX
    PUSH BX
    PUSH CX
    PUSH DX
    CALL func    ; Line 5
    PUSH AX
    POP DX
    POP CX
    POP BX
    MOV BX, [BP - 0]    ; Line 5
    POP AX    ; Line 5
    ADD AX, BX    ; Line 5
    PUSH AX    ; Line 5
    POP AX    ; Line 5
	;restore stack pointer
    ADD SP, 2
	;restore base pointer
    POP BP
    RET 2
func ENDP


new_line proc
    push ax
    push dx
    mov ah,2
    mov dl,0Dh
    int 21h
    mov ah,2
    mov dl,0Ah
    int 21h
    pop dx
    pop ax
    ret
new_line endp
print_output proc  ;print what is in ax
    push ax
    push bx
    push cx
    push dx
    push si
    lea si,number
    mov bx,10
    add si,4
    cmp ax,0
    jnge negate
print:
    xor dx,dx
    div bx
    mov [si],dl
    add [si],'0'
    dec si
    cmp ax,0
    jne print
    inc si
    lea dx,si
    mov ah,9
    int 21h
    pop si
    pop dx
    pop cx
    pop bx
    pop ax
    ret
negate:
    push ax
    mov ah,2
    mov dl,'-'
    int 21h
    pop ax
    neg ax
    jmp print
print_output endp

END
